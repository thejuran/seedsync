---
phase: 05-button-standardization-other-pages
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/angular/src/app/pages/logs/logs-page.component.scss
  - src/angular/src/app/common/_common.scss
autonomous: true

must_haves:
  truths:
    - "Logs page scroll buttons render with Bootstrap styling"
    - "Build succeeds with no SCSS compilation errors"
    - "All Angular unit tests pass"
    - "No @extend %button exists anywhere in the codebase"
  artifacts:
    - path: "src/angular/src/app/common/_common.scss"
      provides: "Common SCSS without %button placeholder"
      contains: "No %button"
    - path: "src/angular/src/app/pages/logs/logs-page.component.scss"
      provides: "Logs SCSS without %button extension"
      contains: "No @extend %button"
  key_links:
    - from: "All component SCSS files"
      to: "_common.scss"
      via: "No %button dependencies"
      pattern: "grep -r '%button' returns nothing in src/angular"
---

<objective>
Remove Logs page %button extension and delete the custom %button placeholder from _common.scss.

Purpose: Complete the button standardization initiative by cleaning up the Logs page SCSS (which already uses Bootstrap buttons in HTML) and removing the now-unused %button placeholder from _common.scss. Verify all Angular tests pass.

Output: Clean SCSS codebase with no custom %button placeholder, all buttons using Bootstrap's btn system.
</objective>

<execution_context>
@/Users/julianamacbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julianamacbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Predecessor summary (Settings + AutoQueue migration)
@.planning/phases/05-button-standardization-other-pages/05-01-SUMMARY.md

# Target files
@src/angular/src/app/pages/logs/logs-page.component.scss
@src/angular/src/app/common/_common.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove @extend %button from Logs page SCSS</name>
  <files>
    src/angular/src/app/pages/logs/logs-page.component.scss
  </files>
  <action>
    The Logs page already uses Bootstrap button elements in HTML (`btn btn-primary btn-scroll`), but the SCSS still extends the custom %button placeholder. Remove the extension.

    **SCSS changes (logs-page.component.scss):**

    In the .btn-scroll selector, change:

    FROM:
    ```scss
    .btn-scroll {
        @extend %button;
        position: sticky;
        display: none;

        &.visible {
            display: inherit;
        }
    }
    ```

    TO:
    ```scss
    .btn-scroll {
        position: sticky;
        display: none;

        &.visible {
            display: block;
        }
    }
    ```

    Key changes:
    - Remove `@extend %button` line
    - Change `display: inherit` to `display: block` (more explicit than inherit)
    - Bootstrap handles all button base styling via the btn btn-primary classes in HTML
  </action>
  <verify>
    Grep for "@extend %button" in logs-page.component.scss - should find nothing.
  </verify>
  <done>
    Logs page SCSS no longer extends the %button placeholder. Bootstrap handles all button styling via HTML classes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove %button placeholder from _common.scss</name>
  <files>
    src/angular/src/app/common/_common.scss
  </files>
  <action>
    Now that no component extends %button, remove the entire placeholder definition from _common.scss.

    **SCSS changes (_common.scss):**

    Remove the entire Button Placeholder section:

    DELETE this block (lines 47-76 approximately):
    ```scss
    // ============================================================================
    // Button Placeholder
    // ============================================================================
    %button {
        background-color: $primary-color;
        color: white;
        border: 1px solid $primary-dark-color;
        border-radius: 4px;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        cursor: default;
        user-select: none;

        &:active {
            // Use Bootstrap's shade-color function (available globally from styles.scss)
            // shade-color($primary-color, 20%) produces #286090 from #337BB7
            background-color: shade-color($primary-color, 20%);
        }

        &[disabled] {
            opacity: .65;
            background-color: $primary-color;
        }

        &.selected {
            background-color: $secondary-color;
            border-color: $secondary-darker-color;
        }
    }
    ```

    The file should end after the z-index variables section.
  </action>
  <verify>
    Grep for "%button" in _common.scss - should find nothing.
    Run `cd /Users/julianamacbook/seedsync/src/angular && npm run build` - should compile without errors.
  </verify>
  <done>
    %button placeholder completely removed from _common.scss. All buttons now use Bootstrap's btn system exclusively.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify no %button references remain and run Angular tests</name>
  <files>None (verification only)</files>
  <action>
    1. Verify no @extend %button or %button definition remains anywhere in the Angular codebase:
       ```bash
       grep -rn "%button" src/angular/src/app/
       ```
       This should return nothing.

    2. Run Angular unit tests to verify nothing broke:
       ```bash
       cd /Users/julianamacbook/seedsync && make run-tests-angular
       ```

    3. If tests fail, investigate the specific failures and fix. Common issues:
       - Template syntax errors (unlikely, we only changed SCSS)
       - Component render issues (would show in console)

    Note: The `make run-tests-angular` command runs tests in Docker and may take a few minutes.
  </action>
  <verify>
    `grep -rn "%button" src/angular/src/app/` returns no results.
    `make run-tests-angular` exits with code 0 (all tests pass).
  </verify>
  <done>
    No %button references exist in codebase. All Angular unit tests pass (387+ tests expected based on Phase 4).
  </done>
</task>

</tasks>

<verification>
1. No %button in Logs SCSS: `grep "@extend %button" src/angular/src/app/pages/logs/logs-page.component.scss` returns nothing
2. No %button in _common.scss: `grep "%button" src/angular/src/app/common/_common.scss` returns nothing
3. No %button anywhere: `grep -rn "%button" src/angular/src/app/` returns nothing
4. Build succeeds: `cd /Users/julianamacbook/seedsync/src/angular && npm run build` completes without errors
5. All tests pass: `make run-tests-angular` exits with code 0
</verification>

<success_criteria>
- Logs page scroll buttons display correctly with Bootstrap styling (no visual regression)
- %button placeholder completely removed from _common.scss
- No @extend %button references remain in any component SCSS
- Angular build completes successfully with no SCSS errors
- All Angular unit tests pass (BTN-11 requirement)
</success_criteria>

<output>
After completion, create `.planning/phases/05-button-standardization-other-pages/05-02-SUMMARY.md`
</output>
