---
phase: 18-controller-unit-tests
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/python/tests/unittests/test_controller/test_controller_unit.py
  - src/python/tests/unittests/test_controller/test_controller_job.py
autonomous: true

must_haves:
  truths:
    - "Model update pipeline is tested: _collect_scan_results delegates to scan_manager, _collect_lftp_status delegates to lftp_manager, _collect_extract_results delegates to file_op_manager"
    - "_feed_model_builder is tested: feeds remote/local/active scans, lftp statuses, extract statuses, and extracted results to model builder, skips None values"
    - "_detect_and_track_queued is tested: tracks DOWNLOADING with local_size>0, ignores QUEUED state, ignores zero/None local_size, ignores already-tracked, handles ADDED vs UPDATED transitions"
    - "_detect_and_track_download is tested: tracks transition to DOWNLOADED state, ignores already-DOWNLOADED"
    - "_prune_extracted_files is tested: removes DELETED files from extracted set, keeps non-DELETED files, keeps files not in model"
    - "_apply_model_diff is tested: applies ADDED/REMOVED/UPDATED changes to internal model"
    - "_update_controller_status is tested: updates context.status with scan timestamps"
    - "_update_active_file_tracking is tested: extracts RUNNING file names from lftp statuses, handles None statuses, passes combined list to scan_manager"
    - "_build_and_apply_model is tested: short-circuits when has_changes is False, triggers full build/diff/apply pipeline when changes exist, calls prune methods after apply"
    - "ControllerJob lifecycle is tested: setup calls controller.start, execute calls controller.process then auto_queue.process in order, cleanup calls controller.exit"
    - "All 711+ existing tests still pass with zero regressions"
  artifacts:
    - path: "src/python/tests/unittests/test_controller/test_controller_unit.py"
      provides: "Additional unit tests for Controller model update pipeline and tracking/pruning"
      contains: "class TestControllerCollect, class TestControllerFeedModelBuilder, class TestControllerDetectAndTrackQueued, class TestControllerDetectAndTrackDownload, class TestControllerPruneExtracted, class TestControllerApplyModelDiff, class TestControllerActiveFileTracking, class TestControllerBuildAndApplyModel, class TestControllerUpdateStatus, class TestControllerPropagateExceptions"
    - path: "src/python/tests/unittests/test_controller/test_controller_job.py"
      provides: "Unit tests for ControllerJob lifecycle (setup/execute/cleanup)"
      contains: "class TestControllerJob"
  key_links:
    - from: "src/python/tests/unittests/test_controller/test_controller_unit.py"
      to: "controller.controller.Controller"
      via: "from controller import Controller"
      pattern: "controller._collect_scan_results(), controller._feed_model_builder(), etc."
    - from: "src/python/tests/unittests/test_controller/test_controller_job.py"
      to: "controller.controller_job.ControllerJob"
      via: "from controller import ControllerJob"
      pattern: "MagicMock controller and auto_queue"
---

<objective>
Create unit tests for the Controller model update pipeline (collect/feed/track/prune/apply/status helper methods), exception propagation, and ControllerJob lifecycle.

Purpose: These tests cover the second half of Controller — the model update pipeline that runs every tick via process(). The refactored helper methods (_collect_*, _feed_model_builder, _detect_and_track_*, _prune_*, _apply_model_diff, _build_and_apply_model, _update_controller_status) are public single-underscore methods designed for direct testing. ControllerJob is a thin wrapper (35 lines) that delegates to Controller and AutoQueue.

Output: Additional test classes appended to test_controller_unit.py (~55 tests) plus a new test_controller_job.py (~5 tests).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.5-ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-controller-unit-tests/18-RESEARCH.md
@.planning/phases/18-controller-unit-tests/18-01-PLAN.md
@src/python/controller/controller.py
@src/python/controller/controller_job.py
@src/python/controller/controller_persist.py
@src/python/model/model.py
@src/python/model/file.py
@src/python/model/diff.py
@src/python/common/job.py
@src/python/tests/conftest.py
@src/python/tests/unittests/test_controller/test_controller_unit.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add model update pipeline and tracking tests to test_controller_unit.py</name>
  <files>
    src/python/tests/unittests/test_controller/test_controller_unit.py
  </files>
  <action>
  Append additional test classes to the existing test_controller_unit.py file. All classes extend `BaseControllerTestCase` from Plan 1.

  **Additional imports to add at top of file (if not already present):**
  ```python
  from model import ModelDiff
  from lftp import LftpJobStatus
  ```

  **Class `TestControllerCollect(BaseControllerTestCase)`:**
  Tests the _collect_* delegation methods.

  - `test_collect_scan_results_delegates_to_scan_manager`: Call `self.controller._collect_scan_results()`. Verify `self.mock_scan_manager.pop_latest_results.assert_called_once()`. Verify return value matches mock return.
  - `test_collect_lftp_status_delegates_to_lftp_manager`: Set `self.mock_lftp_manager.status.return_value = [MagicMock()]`. Call `self.controller._collect_lftp_status()`. Verify delegation and return.
  - `test_collect_extract_results_delegates_to_file_op_manager`: Set return values for `pop_extract_statuses` and `pop_completed_extractions`. Call `self.controller._collect_extract_results()`. Verify both delegations.

  **Class `TestControllerFeedModelBuilder(BaseControllerTestCase)`:**
  Tests _feed_model_builder with various combinations of inputs.

  - `test_feed_remote_scan`: Create `mock_scan` with `.files` attribute. Call `self.controller._feed_model_builder(remote_scan=mock_scan, local_scan=None, active_scan=None, lftp_statuses=None, extract_statuses=None, extracted_results=[])`. Verify `self.mock_model_builder.set_remote_files.assert_called_once_with(mock_scan.files)`.
  - `test_feed_local_scan`: Similar, verify `set_local_files` called.
  - `test_feed_active_scan`: Similar, verify `set_active_files` called.
  - `test_feed_lftp_statuses`: Create mock statuses list. Verify `set_lftp_statuses` called.
  - `test_feed_extract_statuses`: Create mock with `.statuses` attribute. Verify `set_extract_statuses` called.
  - `test_feed_extracted_results_updates_persist`: Create mock result with `.name = "extracted_file"`. Call `_feed_model_builder(..., extracted_results=[result])`. Verify `"extracted_file" in self.persist.extracted_file_names`. Verify `self.mock_model_builder.set_extracted_files.assert_called()`.
  - `test_feed_all_none_does_nothing`: Call with all None/empty params. Verify no `set_*` methods called on model builder (except `set_extracted_files` is not called since `extracted_results` is empty).
  - `test_feed_multiple_extracted_results`: Create 2 mock results. Verify both names added to persist.

  **Class `TestControllerDetectAndTrackQueued(BaseControllerTestCase)`:**
  Tests _detect_and_track_queued with various diff scenarios.

  - `test_added_downloading_with_content_is_tracked`: Create `ModelFile("file", False)` with `state=DOWNLOADING, local_size=1000`. Create `ModelDiff(ADDED, None, file)`. Call `self.controller._detect_and_track_queued(diff)`. Verify `"file" in self.persist.downloaded_file_names`.
  - `test_added_queued_is_not_tracked`: File with `state=QUEUED, local_size=0`. Diff ADDED. Call. Verify NOT in downloaded.
  - `test_added_downloading_zero_local_size_not_tracked`: File with `state=DOWNLOADING, local_size=0`. Diff ADDED. Call. Verify NOT tracked.
  - `test_added_downloading_none_local_size_not_tracked`: File with `state=DOWNLOADING`. (local_size is None by default). Diff ADDED. Call. Verify NOT tracked.
  - `test_already_tracked_not_re_added`: Add "file" to `self.persist.downloaded_file_names`. Create downloading file with content. Diff ADDED. Call. Verify `self.mock_model_builder.set_downloaded_files` NOT called (because file was already tracked, so set_downloaded_files should not be called again in this invocation).
  - `test_updated_from_default_to_downloading_with_content_is_tracked`: Create old_file with `state=DEFAULT`. Create new_file with `state=DOWNLOADING, local_size=1000`. Diff UPDATED. Call. Verify tracked.
  - `test_updated_from_downloading_with_no_content_to_content_is_tracked`: old_file `state=DOWNLOADING, local_size=0`. new_file `state=DOWNLOADING, local_size=1000`. Diff UPDATED. Call. Verify tracked.
  - `test_removed_diff_not_tracked`: Create a diff with `change=REMOVED, old_file=file, new_file=None`. Call. Verify no change to persist (new_file is None, method returns early).

  **Class `TestControllerDetectAndTrackDownload(BaseControllerTestCase)`:**
  Tests _detect_and_track_download.

  - `test_added_downloaded_is_tracked`: File `state=DOWNLOADED`. Diff ADDED. Call `self.controller._detect_and_track_download(diff)`. Verify tracked in persist.
  - `test_updated_to_downloaded_is_tracked`: old_file `state=DOWNLOADING`. new_file `state=DOWNLOADED`. Diff UPDATED. Call. Verify tracked.
  - `test_updated_already_downloaded_not_re_tracked`: old_file `state=DOWNLOADED`. new_file `state=DOWNLOADED`. Diff UPDATED. Call. Verify `set_downloaded_files` NOT called.
  - `test_added_non_downloaded_not_tracked`: File `state=QUEUED`. Diff ADDED. Call. Verify NOT tracked.
  - `test_removed_not_tracked`: Diff REMOVED. Call. Verify no change.
  - `test_updated_to_non_downloaded_not_tracked`: old_file `state=DOWNLOADING`. new_file `state=QUEUED`. Diff UPDATED. Call. Verify NOT tracked.

  **Class `TestControllerPruneExtracted(BaseControllerTestCase)`:**
  Tests _prune_extracted_files.

  - `test_prune_removes_deleted_file`: Add "file" to `self.persist.extracted_file_names`. Add file to model with `state=DELETED`. Call `self.controller._prune_extracted_files()`. Verify `"file" not in self.persist.extracted_file_names`.
  - `test_prune_keeps_non_deleted_file`: Add "file" to persist. Add file to model with `state=DOWNLOADED`. Call. Verify "file" still in persist.
  - `test_prune_keeps_file_not_in_model`: Add "orphan" to persist. Don't add to model. Call. Verify "orphan" still in persist (scans may not be available yet).
  - `test_prune_empty_extracted_set`: Call with empty persist. No crash.

  **Class `TestControllerApplyModelDiff(BaseControllerTestCase)`:**
  Tests _apply_model_diff.

  - `test_apply_added_file`: Create file. Create diff `ADDED, None, file`. Call `self.controller._apply_model_diff([diff])`. Verify file is in `self.controller._Controller__model` via `get_file("file")`.
  - `test_apply_removed_file`: Add file to model first. Create diff `REMOVED, file, None`. Call. Verify `get_file("file")` raises ModelError.
  - `test_apply_updated_file`: Add file to model. Create new_file with different state. Create diff `UPDATED, old_file, new_file`. Call. Verify `get_file("file")` returns updated version.
  - `test_apply_mixed_diff`: Add file_a. Create: diff_remove_a (REMOVED), diff_add_b (ADDED), diff_add_c (ADDED). Call with list of all 3. Verify: file_a gone, file_b and file_c present.
  - `test_apply_empty_diff`: Call with empty list. No crash, model unchanged.
  - `test_apply_diff_triggers_tracking`: Create file with `state=DOWNLOADED`. Create diff ADDED. Call. Verify file tracked in persist (because _apply_model_diff also calls _detect_and_track_queued and _detect_and_track_download).

  **Class `TestControllerActiveFileTracking(BaseControllerTestCase)`:**
  Tests _update_active_file_tracking.

  - `test_lftp_statuses_updates_downloading_names`: Create 2 mock LftpJobStatus with `state=LftpJobStatus.State.RUNNING, name="file_a"` and `state=LftpJobStatus.State.RUNNING, name="file_b"`. Call `self.controller._update_active_file_tracking(lftp_statuses=[status_a, status_b], extract_statuses=None)`. Verify `self.controller._Controller__active_downloading_file_names == ["file_a", "file_b"]`.
  - `test_lftp_statuses_filters_non_running`: Create mock with `state=LftpJobStatus.State.QUEUED`. Call. Verify `self.controller._Controller__active_downloading_file_names == []`.
  - `test_lftp_statuses_none_preserves_existing`: Set `self.controller._Controller__active_downloading_file_names = ["old_file"]`. Call with `lftp_statuses=None`. Verify list still equals `["old_file"]` (no change when None).
  - `test_calls_scan_manager_update_active_files`: Create running status with name "dl_file". Call. Verify `self.mock_scan_manager.update_active_files.assert_called_once()`. The argument should be a combined list of downloading + extracting file names.

  **Class `TestControllerBuildAndApplyModel(BaseControllerTestCase)`:**
  Tests _build_and_apply_model orchestration.

  - `test_no_changes_skips_build`: `self.mock_model_builder.has_changes.return_value = False`. Call `self.controller._build_and_apply_model(remote_scan=None)`. Verify `self.mock_model_builder.build_model.assert_not_called()`.
  - `test_has_changes_triggers_build`: `self.mock_model_builder.has_changes.return_value = True`. Create a mock new_model (MagicMock with `get_file_names` returning empty set). `self.mock_model_builder.build_model.return_value = new_model`. Call `self.controller._build_and_apply_model(remote_scan=None)`. Verify `self.mock_model_builder.build_model.assert_called_once()`.
  - `test_has_changes_applies_diff_to_model`: `self.mock_model_builder.has_changes.return_value = True`. Create new_model with `get_file_names` returning `{"new_file"}`. Create a ModelFile "new_file" and add it to new_model's `get_file` return. `self.mock_model_builder.build_model.return_value = new_model`. Need to use a real Model for new_model to make ModelDiffUtil.diff_models work: create `new_model = Model()`, add `ModelFile("new_file", False)` to it. Call `_build_and_apply_model(None)`. Verify "new_file" is now in `self.controller._Controller__model.get_file_names()`.
  - `test_has_changes_no_diff_no_change`: `self.mock_model_builder.has_changes.return_value = True`. Return an empty Model (no files). Call. Model should remain empty.

  **Class `TestControllerUpdateStatus(BaseControllerTestCase)`:**
  Tests _update_controller_status.

  - `test_update_status_with_remote_scan`: Create mock_scan with `timestamp`, `failed`, `error_message`. Call `self.controller._update_controller_status(remote_scan=mock_scan, local_scan=None)`. Verify `self.mock_context.status.controller.latest_remote_scan_time == mock_scan.timestamp`.
  - `test_update_status_with_local_scan`: Create mock_scan with `timestamp`. Call with `local_scan=mock_scan`. Verify `self.mock_context.status.controller.latest_local_scan_time == mock_scan.timestamp`.
  - `test_update_status_with_both_none`: Call with both None. No crash, no attribute assignments.

  **Class `TestControllerPropagateExceptions(BaseControllerTestCase)`:**
  Tests __propagate_exceptions (called during process).

  - `test_propagate_calls_all_managers`: `self._make_controller_started()`. `self.controller.process()`. Verify: `self.mock_lftp_manager.raise_pending_error.assert_called_once()`, `self.mock_scan_manager.propagate_exceptions.assert_called_once()`, `self.mock_mp_logger.propagate_exception.assert_called_once()`, `self.mock_file_op_manager.propagate_exception.assert_called_once()`.
  - `test_propagate_lftp_error_raises`: `self._make_controller_started()`. Set `self.mock_lftp_manager.raise_pending_error.side_effect = LftpError("error")`. `with self.assertRaises(LftpError): self.controller.process()`.
  - `test_process_calls_cleanup_completed`: `self._make_controller_started()`. `self.controller.process()`. Verify `self.mock_file_op_manager.cleanup_completed_processes.assert_called_once()`.
  </action>
  <verify>
  Run from src/python/:
  - `cd /Users/julianamacbook/seedsync/src/python && /Users/julianamacbook/Library/Python/3.12/bin/poetry run pytest tests/unittests/test_controller/test_controller_unit.py -v -k "Collect or Feed or Track or Prune or Apply or Active or Build or Status or Propagate"`
  All new tests must pass. Expected: ~55 additional test methods, 0 failures.
  </verify>
  <done>
  10 additional test classes appended to test_controller_unit.py covering: collect delegation, feed model builder, detect and track queued (ADDED/UPDATED transitions, boundary conditions), detect and track download (state transitions), prune extracted files, apply model diff (ADDED/REMOVED/UPDATED + tracking), active file tracking, build and apply model orchestration, update controller status, and propagate exceptions.
  All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test_controller_job.py and run full regression</name>
  <files>
    src/python/tests/unittests/test_controller/test_controller_job.py
  </files>
  <action>
  Create a new test file for ControllerJob lifecycle. ControllerJob is a thin wrapper (35 lines) that delegates to Controller and AutoQueue via the Job base class.

  **Copyright header:** `# Copyright 2017, Inderpreet Singh, All rights reserved.`

  **File content:**
  ```python
  # Copyright 2017, Inderpreet Singh, All rights reserved.

  import unittest
  from unittest.mock import MagicMock, call

  from controller import ControllerJob


  class TestControllerJob(unittest.TestCase):
      def setUp(self):
          self.mock_context = MagicMock()
          self.mock_context.logger = MagicMock()
          self.mock_controller = MagicMock()
          self.mock_auto_queue = MagicMock()
          self.job = ControllerJob(
              context=self.mock_context,
              controller=self.mock_controller,
              auto_queue=self.mock_auto_queue
          )

      def test_setup_starts_controller(self):
          self.job.setup()
          self.mock_controller.start.assert_called_once()

      def test_execute_processes_controller(self):
          self.job.execute()
          self.mock_controller.process.assert_called_once()

      def test_execute_processes_auto_queue(self):
          self.job.execute()
          self.mock_auto_queue.process.assert_called_once()

      def test_execute_calls_controller_before_auto_queue(self):
          call_order = []
          self.mock_controller.process.side_effect = lambda: call_order.append('controller')
          self.mock_auto_queue.process.side_effect = lambda: call_order.append('auto_queue')

          self.job.execute()

          self.assertEqual(['controller', 'auto_queue'], call_order)

      def test_cleanup_exits_controller(self):
          self.job.cleanup()
          self.mock_controller.exit.assert_called_once()
  ```

  **After creating the file, run full regression.**
  </action>
  <verify>
  Run from src/python/:

  1. Run only the new ControllerJob tests:
  `cd /Users/julianamacbook/seedsync/src/python && /Users/julianamacbook/Library/Python/3.12/bin/poetry run pytest tests/unittests/test_controller/test_controller_job.py -v`
  Expected: 5 test methods, all passing.

  2. Run ALL controller unit tests together:
  `cd /Users/julianamacbook/seedsync/src/python && /Users/julianamacbook/Library/Python/3.12/bin/poetry run pytest tests/unittests/test_controller/test_controller_unit.py tests/unittests/test_controller/test_controller_job.py -v`
  Expected: ~110 total test methods, all passing.

  3. Run ALL tests to confirm zero regressions:
  `cd /Users/julianamacbook/seedsync/src/python && /Users/julianamacbook/Library/Python/3.12/bin/poetry run pytest -q 2>&1 | tail -5`
  Must show 711+ tests with no NEW failures.
  </verify>
  <done>
  test_controller_job.py has TestControllerJob with 5 test methods covering: setup starts controller, execute processes controller, execute processes auto_queue, execute calls controller before auto_queue (order verified), cleanup exits controller.
  All controller tests pass together (~110 test methods across 2 files).
  All 711+ existing tests still pass with zero regressions.
  </done>
</task>

</tasks>

<verification>
Phase-level verification for Plan 2 after all tasks complete:

1. **Both test files exist:**
   `ls src/python/tests/unittests/test_controller/test_controller_unit.py src/python/tests/unittests/test_controller/test_controller_job.py`

2. **All controller tests pass together:**
   `cd src/python && /Users/julianamacbook/Library/Python/3.12/bin/poetry run pytest tests/unittests/test_controller/test_controller_unit.py tests/unittests/test_controller/test_controller_job.py -v`
   Expected: ~110 tests, all passing.

3. **Zero regressions in full suite:**
   `cd src/python && /Users/julianamacbook/Library/Python/3.12/bin/poetry run pytest -q`
   Expected: 711+ tests total, same pre-existing failure count.

4. **Copyright headers present:**
   Both files start with `# Copyright 2017, Inderpreet Singh, All rights reserved.`

5. **Pattern compliance:**
   - test_controller_unit.py: BaseControllerTestCase with patch.start()/stop(), real ControllerPersist, direct ModelFile creation
   - test_controller_job.py: Simple unittest.TestCase with MagicMock for injected Controller and AutoQueue
   - Helper methods tested directly (single-underscore public API)
   - No integration test patterns
</verification>

<success_criteria>
- 2 test files: test_controller_unit.py (expanded) and test_controller_job.py (new)
- ~110 total test methods across ~18 test classes
- Model update pipeline: collect/feed/track/prune/apply/active-tracking/build-apply/status all tested
- _detect_and_track_queued: 8 edge cases covering state/size/already-tracked/transition type
- _detect_and_track_download: 6 edge cases covering state transitions
- _prune_extracted_files: DELETED removal, non-DELETED retention, orphan retention
- _apply_model_diff: ADDED/REMOVED/UPDATED + mixed + tracking integration
- ControllerJob: all 3 lifecycle methods + execution order verified
- All new tests pass
- All 711+ existing tests still pass (zero regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/18-controller-unit-tests/18-02-SUMMARY.md`

The SUMMARY must include:
- Exact count of new test methods added (in both files)
- Exact count of test classes
- Full test suite pass/fail counts (showing zero new regressions)
- Any deviations from plan
</output>
