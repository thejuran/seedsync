---
phase: 04-button-standardization-file-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/angular/src/app/pages/files/file-actions-bar.component.html
  - src/angular/src/app/pages/files/file-actions-bar.component.scss
  - src/angular/src/app/pages/files/bulk-actions-bar.component.html
  - src/angular/src/app/pages/files/bulk-actions-bar.component.scss
autonomous: true

must_haves:
  truths:
    - "Stop button displays as red (btn-danger) in both action bars"
    - "Extract button displays as gray (btn-secondary) in both action bars"
    - "Delete Local button displays as solid red (btn-danger) in both action bars"
    - "All buttons use Bootstrap default sizing (~38px height)"
    - "Button clicks trigger correct actions (no functional regressions)"
  artifacts:
    - path: "src/angular/src/app/pages/files/file-actions-bar.component.html"
      provides: "File action buttons with correct Bootstrap variants"
      contains: "btn-danger"
    - path: "src/angular/src/app/pages/files/bulk-actions-bar.component.html"
      provides: "Bulk action buttons with correct Bootstrap variants"
      contains: "btn-secondary"
  key_links:
    - from: "file-actions-bar.component.html"
      to: "file-actions-bar.component.ts"
      via: "(click) event handlers"
      pattern: "\\(click\\)=\"on"
    - from: "bulk-actions-bar.component.html"
      to: "bulk-actions-bar.component.ts"
      via: "(click) event handlers"
      pattern: "\\(click\\)=\"on"
---

<objective>
Migrate file-actions-bar and bulk-actions-bar button variants from incorrect Bootstrap classes to correct semantic variants per context decisions.

Purpose: Establish consistent button semantic mapping across all file action UI components. Stop = danger (red), Extract = secondary (gray), Delete Local = solid danger (not outline).

Output: Both action bar components using correct Bootstrap button variants with default sizing.
</objective>

<execution_context>
@/Users/julianamacbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julianamacbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-button-standardization-file-actions/04-CONTEXT.md
@.planning/phases/04-button-standardization-file-actions/04-RESEARCH.md
@src/angular/src/app/pages/files/file-actions-bar.component.html
@src/angular/src/app/pages/files/file-actions-bar.component.scss
@src/angular/src/app/pages/files/bulk-actions-bar.component.html
@src/angular/src/app/pages/files/bulk-actions-bar.component.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate file-actions-bar button variants and sizing</name>
  <files>
    src/angular/src/app/pages/files/file-actions-bar.component.html
    src/angular/src/app/pages/files/file-actions-bar.component.scss
  </files>
  <action>
In file-actions-bar.component.html:

1. Change Stop button from `btn btn-sm btn-warning` to `btn btn-danger`
2. Change Extract button from `btn btn-sm btn-info` to `btn btn-secondary`
3. Change Delete Local button from `btn btn-sm btn-outline-danger` to `btn btn-danger`
4. Change Queue button from `btn btn-sm btn-primary` to `btn btn-primary`
5. Change Delete Remote button from `btn btn-sm btn-danger` to `btn btn-danger`

Rationale:
- Remove all `btn-sm` classes - context specifies Bootstrap default sizing (~38px)
- Stop = btn-danger (red) not btn-warning (yellow) per context
- Extract = btn-secondary (gray) not btn-info (cyan) per context
- Delete Local = btn-danger (solid) not btn-outline-danger per context

In file-actions-bar.component.scss:

1. Verify icon styling remains intact (filter: invert(1) for white icons on colored backgrounds)
2. No changes needed to SCSS if icon styling is already correct
  </action>
  <verify>
1. Run: `grep -n "btn-warning\|btn-info\|btn-outline-danger\|btn-sm" src/angular/src/app/pages/files/file-actions-bar.component.html` returns no results
2. Run: `grep -n "btn-danger" src/angular/src/app/pages/files/file-actions-bar.component.html` returns 3 lines (Stop, Delete Local, Delete Remote)
3. Run: `grep -n "btn-secondary" src/angular/src/app/pages/files/file-actions-bar.component.html` returns 1 line (Extract)
  </verify>
  <done>
file-actions-bar buttons use correct variants: Queue=primary, Stop=danger, Extract=secondary, Delete Local=danger, Delete Remote=danger. No btn-sm classes remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate bulk-actions-bar button variants and sizing</name>
  <files>
    src/angular/src/app/pages/files/bulk-actions-bar.component.html
    src/angular/src/app/pages/files/bulk-actions-bar.component.scss
  </files>
  <action>
In bulk-actions-bar.component.html:

1. Change Stop button from `btn btn-sm btn-warning action-btn` to `btn btn-danger action-btn`
2. Change Extract button from `btn btn-sm btn-info action-btn` to `btn btn-secondary action-btn`
3. Change Delete Local button from `btn btn-sm btn-outline-danger action-btn` to `btn btn-danger action-btn`
4. Change Queue button from `btn btn-sm btn-primary action-btn` to `btn btn-primary action-btn`
5. Change Delete Remote button from `btn btn-sm btn-danger action-btn` to `btn btn-danger action-btn`

Keep the `action-btn` class for component-specific styling.

Rationale:
- Same variant mapping as file-actions-bar for consistency
- Remove all `btn-sm` classes - context specifies Bootstrap default sizing

In bulk-actions-bar.component.scss:

1. No changes expected - styling uses Bootstrap defaults
2. Verify action-btn styles don't conflict with new sizing
  </action>
  <verify>
1. Run: `grep -n "btn-warning\|btn-info\|btn-outline-danger\|btn-sm" src/angular/src/app/pages/files/bulk-actions-bar.component.html` returns no results
2. Run: `grep -n "btn-danger" src/angular/src/app/pages/files/bulk-actions-bar.component.html` returns 3 lines (Stop, Delete Local, Delete Remote)
3. Run: `grep -n "btn-secondary" src/angular/src/app/pages/files/bulk-actions-bar.component.html` returns 1 line (Extract)
4. Run: `cd /Users/julianamacbook/seedsync && make run-tests-angular` passes (catches SCSS compile errors)
  </verify>
  <done>
bulk-actions-bar buttons use correct variants matching file-actions-bar. No btn-sm classes remain. Angular tests pass.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Variant check:** `grep -rn "btn-warning\|btn-info\|btn-outline" src/angular/src/app/pages/files/` returns no results for action bar components
2. **Sizing check:** `grep -rn "btn-sm" src/angular/src/app/pages/files/*-bar.component.html` returns no results
3. **Build check:** `make run-tests-angular` passes without errors
</verification>

<success_criteria>
- All file action bar buttons use correct Bootstrap semantic variants
- Stop buttons are red (btn-danger) not yellow
- Extract buttons are gray (btn-secondary) not cyan
- Delete Local buttons are solid red (btn-danger) not outline
- All buttons use Bootstrap default sizing (no btn-sm)
- Existing click handlers continue to work (no Angular/TS changes)
- Icon styling preserved (white icons on colored backgrounds)
</success_criteria>

<output>
After completion, create `.planning/phases/04-button-standardization-file-actions/04-01-SUMMARY.md`
</output>
